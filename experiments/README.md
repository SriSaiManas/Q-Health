# Runtime experiment artifacts

No results are bundled. Actual experiment/job/model metadata is stored in SQLite when you run the application. Private JSON snapshots and requested HTML reports are written here under generated experiment UUIDs. Reports contain measured results or explicit unavailability, not fabricated demo values. Protect exports and source metadata according to your research-data policy.
