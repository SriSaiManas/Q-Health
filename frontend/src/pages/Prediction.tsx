import {useEffect, useRef, useState} from 'react';
import {api} from '../services/api';
import {useAction, useLoad} from '../hooks/useLoad';
import type {Prediction as PredictionResult} from '../types';
import {Card, Disclaimer, Empty, ErrorNotice, Field, JsonView, ModelPicker, Note, PageHeader} from '../components/Common';
import {BarChart} from '../components/Charts';
import {metric, modelLabels} from '../utils/format';
interface InputSchema {model_id: string; features: {name: string; type: string; nullable: boolean}[]; positive_label: string; negative_label: string;}
export default function Prediction() {
  const [id, setId] = useState(''); const [values, setValues] = useState<Record<string, string>>({}); const [low, setLow] = useState(0.33); const [high, setHigh] = useState(0.66); const [influence, setInfluence] = useState(true); const [prediction, setPrediction] = useState<PredictionResult>(); const action = useAction();
  const schema = useLoad<InputSchema | null>(() => id ? api.get(`/models/${id}/input-schema`) : Promise.resolve(null), [id]);
  const latest = useRef(''); latest.current = JSON.stringify({id, values, low, high, influence});
  const latestModel = useRef(id); latestModel.current = id;
  useEffect(() => {setValues({}); setPrediction(undefined);}, [id]);
  async function demo() {const requestedId = id; const value = await action.run(() => api.get<{features: Record<string, string | number | null>}>(`/models/${id}/demo-sample`)); if (value && latestModel.current === requestedId) {setValues(Object.fromEntries(Object.entries(value.features).map(([k, v]) => [k, v === null ? '' : String(v)]))); setPrediction(undefined);}}
  async function run() {
    if (!schema.data || schema.data.model_id !== id) return;
    const signature = latest.current; const requestedId = id;
    const sample = Object.fromEntries(schema.data.features.map(f => [f.name, values[f.name] === undefined || values[f.name] === '' ? null : f.type === 'number' ? Number(values[f.name]) : values[f.name]]));
    const result = await action.run(() => api.post<PredictionResult>(`/models/${id}/predict`, {samples: [sample], risk_thresholds: [low, high], include_influence: influence}));
    if (result && latest.current === signature && result.model_id === requestedId) setPrediction(result);
  }
  return <><PageHeader eyebrow="10 / Research prediction" title="Biomedical risk prediction." description="Submit an anonymous sample using a registered model. Model-generated classification is research output, not a confirmed diagnosis or a medical recommendation."/><Disclaimer/><ModelPicker value={id} onChange={setId}/><ErrorNotice message={schema.error || action.error}/>
    <div className="two-columns"><Card title="Anonymous sample features"><Note>Values remain in this page's memory and are not persisted by the prediction API. Empty values use the model's training-fitted imputation. Do not enter identifiers.</Note>{schema.data ? <><button className="secondary" disabled={!id || action.busy} onClick={() => void demo()}>Load one public benchmark sample</button><small>This is available only for models trained on the built-in public dataset; it never retrieves uploaded records.</small><div className="form-grid sample-fields">{schema.data.features.map(f => <Field label={f.name} key={f.name}><input type={f.type === 'number' ? 'number' : 'text'} step="any" maxLength={200} value={values[f.name] || ''} placeholder="Missing / impute" onChange={e => {setValues({...values, [f.name]: e.target.value}); setPrediction(undefined);}}/></Field>)}</div></> : <Empty>Select a registered model to load its exact input schema.</Empty>}</Card>
    <div><Card title="Research thresholds"><div className="form-grid"><Field label="Low / medium boundary"><input type="number" min={0.01} max={0.98} step={0.01} value={low} onChange={e => {setLow(Number(e.target.value)); setPrediction(undefined);}}/></Field><Field label="Medium / high boundary"><input type="number" min={0.02} max={0.99} step={0.01} value={high} onChange={e => {setHigh(Number(e.target.value)); setPrediction(undefined);}}/></Field></div><p>These are demonstration / research risk categories, not clinically validated thresholds. They apply to the configured positive class: <b>{schema.data?.positive_label || 'Not selected'}</b>.</p><label className="check"><input type="checkbox" checked={influence} onChange={e => setInfluence(e.target.checked)}/>Calculate local feature perturbation (additional simulator work for quantum models).</label><button disabled={!schema.data || schema.data.model_id !== id || action.busy || !(0 < low && low < high && high < 1)} onClick={() => void run()}>{action.busy ? 'Calculating research prediction...' : 'Generate research prediction'}</button></Card>
    {prediction && <Card title="Model-generated result">{prediction.predictions.map(p => <div className="prediction-result" key={p.sample}><p className="eyebrow">{p.sample} / {modelLabels[prediction.model_type]}</p><h3>Predicted class: {p.predicted_class}</h3><dl><dt>Positive-class model probability</dt><dd>{p.probability_positive === null ? 'Not available: margin-only model' : metric(p.probability_positive)}</dd><dt>Decision score</dt><dd>{p.decision_score === null ? 'Probability-based model' : p.decision_score.toFixed(4)}</dd><dt>Research risk category</dt><dd>{p.research_risk_category || 'Not assigned without probability output'}</dd></dl><p>{prediction.probability_status}</p><p>The selected model classified this sample as <b>{p.predicted_class}</b> based on learned patterns in the provided biomedical features. This is not a clinical diagnosis.</p></div>)}<JsonView value={{decision_rule: prediction.decision_rule, limitations: prediction.limitations}} label="Decision rule and research limitations"/></Card>}</div></div>
    {prediction?.influence && <Card title="Local model feature influence"><p>Measured prediction change under feature perturbation; no medical causation or treatment implication.</p><BarChart label="Local feature perturbation influence" items={prediction.influence.slice(0, 20).map(i => ({name: i.feature, value: i.magnitude}))}/><JsonView value={prediction.influence} label="All evaluated perturbations"/></Card>}</>;
}
