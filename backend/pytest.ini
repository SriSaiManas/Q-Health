[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    quantum: optional quantum execution, requires RUN_QUANTUM_TESTS=1
    integration: actual local HTTP-to-training workflow (not run during generation)
